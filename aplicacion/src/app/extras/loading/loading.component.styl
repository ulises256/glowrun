
.cdk-overlay-pane
    width 100vw
    height 100vh


.mat-dialog-container
    display:  flex
    background-color: rgba(0,0,0, 0.1);
    justify-content: center;
    align-items: center;
    position: relative;
    
mat-spinner
    position absolute